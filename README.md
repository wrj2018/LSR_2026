# LSR
This code is used to analyze the data of LSR values.

### Setting Up the Environment for This Project

It is recommended to use conda, please check `environment.txt`. You may also do `pip install -r requirements.txt`.

### Reproducing the workflow
To run the code as a whole workflow, do:
```bash workflow.sh```
